(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
const apiJson=async(path,opts={})=>{const r=await fetch(path,{credentials:'same-origin',headers:{'content-type':'application/json'},...opts});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Не удалось выполнить запрос');return d};
const subjectLabel=s=>s==='chemistry'?'Химия':'Биология';
function syncRouteClass(){document.documentElement.classList.toggle('osnova-simple-dashboard',(location.hash.slice(1)||'dashboard')==='dashboard')}
syncRouteClass();addEventListener('hashchange',syncRouteClass);
if(typeof adminTabs!=='function'||typeof admin!=='function'||typeof adminFrame!=='function')return;
const originalAdmin=admin;
adminTabs=()=>`<div class="admin-tabs">${[['overview','Обзор'],['tools','Инструменты'],['content','Контент'],['questions','Задания'],['users','Пользователи'],['site','Сайт']].map(([id,label])=>`<button class="${adminTab===id?'active':''}" data-admin-tab="${id}">${label}</button>`).join('')}</div>`;
admin=async function(){if(adminTab==='tools')return adminTools();return originalAdmin();};
async function loadHub(){const [bio,chem]=await Promise.all([apiJson('/api/ai-pro/plan?subject=biology'),apiJson('/api/ai-pro/plan?subject=chemistry')]);const plans=[bio.plan,chem.plan].filter(Boolean).sort((a,b)=>Date.parse(b.generatedAt||0)-Date.parse(a.generatedAt||0));const plan=plans[0]||bio.plan||chem.plan||null;const subject=plan?.subjectSlug||'biology';const [tutor,progress,review]=await Promise.all([apiJson(`/api/ai-pro/tutor/today?subject=${subject}`).catch(()=>({tutor:null})),apiJson(`/api/ai-pro/progress?subject=${subject}`).catch(()=>({current:{}})),apiJson(`/api/ai-pro/review-status?subject=${subject}`).catch(()=>({}))]);return{plan,subject,tutor:tutor.tutor||tutor,progress,review};}
function score(plan){if(!plan||plan.scoreEstimate==null)return'Недостаточно данных';return plan.scoreRange?.low!=null?`${plan.scoreRange.low}–${plan.scoreRange.high}`:String(plan.scoreEstimate)}
function tutorSteps(t){return (t?.steps||[]).map(x=>`<div class="tool-step ${x.done?'done':''}"><span>${x.done?'✓':'•'}</span><div><b>${esc(x.title||x.key)}</b><small>${esc(x.subtitle||x.reason||'')}</small></div></div>`).join('')||'<p class="subtitle">План занятия появится после настройки AI PRO.</p>'}
function hubHtml(d){const p=d.plan,t=d.tutor,c=d.progress.current||{},r=d.review||{};return `<div id="admin-tools-hub" class="admin-tools-hub"><section class="tool-card tool-pro"><div class="tool-head"><div><span class="eyebrow">OSNOVA AI PRO</span><h2>Персональный план</h2></div><span class="pill">${subjectLabel(d.subject)}</span></div><div class="tool-stats"><div><span>Прогноз</span><b>${esc(score(p))}</b></div><div><span>Готовность</span><b>${p?.readiness??0}%</b></div><div><span>Покрытие</span><b>${p?.coverage??0}%</b></div><div><span>Цель</span><b>${p?.targetScore??85}+</b></div></div><div class="tool-actions"><button class="btn" data-tool-plan>${p?'Обновить план':'Настроить AI PRO'}</button><button class="btn ghost" data-tool-diagnostic>Диагностика</button></div><form class="tool-plan-form" hidden><select name="subjectSlug"><option value="biology" ${d.subject==='biology'?'selected':''}>Биология</option><option value="chemistry" ${d.subject==='chemistry'?'selected':''}>Химия</option></select><input name="targetScore" type="number" min="40" max="100" value="${p?.targetScore||85}" placeholder="Цель"><input name="examDate" type="date" value="${p?.examDate||''}" required><input name="daysPerWeek" type="number" min="1" max="7" value="${p?.daysPerWeek||5}" placeholder="Дней"><input name="minutesPerDay" type="number" min="20" max="300" value="${p?.minutesPerDay||60}" placeholder="Минут"><button class="btn">Построить план</button></form></section><section class="tool-card"><div class="tool-head"><div><span class="eyebrow">Цифровой репетитор</span><h2>Занятие на сегодня</h2></div><b>${t?.progress??0}%</b></div><div class="tool-steps">${tutorSteps(t)}</div><button class="btn block" data-tool-tutor ${t?.nextStep?'':'disabled'}>${t?.nextStep?'Продолжить занятие →':'На сегодня всё выполнено'}</button></section><section class="tool-card"><div class="tool-head"><div><span class="eyebrow">Динамика</span><h2>Неделя и повторение</h2></div></div><div class="tool-stats"><div><span>Заданий</span><b>${c.solved||0}</b></div><div><span>Точность</span><b>${c.accuracy||0}%</b></div><div><span>Практика</span><b>${c.minutes||0} мин</b></div><div><span>Повторить</span><b>${r.dueCount||0}</b></div></div><button class="btn ghost block" data-tool-review ${r.dueCount?'':'disabled'}>${r.dueCount?'Начать умное повторение':'Повторений пока нет'}</button></section><section class="tool-card"><div class="tool-head"><div><span class="eyebrow">Контроль ЕГЭ</span><h2>Карта линий и режим 90+</h2></div></div><p class="subtitle">Карта прогресса и режим 90+ теперь открываются отдельно и не перегружают главную.</p><div class="tool-actions"><button class="btn" data-map="biology">Карта биологии</button><button class="btn ghost" data-map="chemistry">Карта химии</button></div></section></div>`}
async function adminTools(){adminFrame('<div class="card admin-loading">Загружаем учебные инструменты…</div>','Инструменты');try{const d=await loadHub();adminFrame(hubHtml(d),'Инструменты');bindHub(d)}catch(e){adminFrame(`<div class="card"><h2>Не удалось загрузить инструменты</h2><p class="subtitle">${esc(e.message)}</p><button class="btn" data-tools-retry>Повторить</button></div>`,'Инструменты');$('[data-tools-retry]')?.addEventListener('click',adminTools)}}
async function startSession(path,body,subject){const d=await apiJson(path,{method:'POST',body:JSON.stringify(body)});if(!d.session?.id)throw new Error('Не удалось начать занятие');sessionStorage.trainingSession=d.session.id;sessionStorage.trainingSubject=subject;location.hash='training'}
function bindHub(d){$('[data-tool-plan]')?.addEventListener('click',()=>{const f=$('.tool-plan-form');f.hidden=!f.hidden});$('.tool-plan-form')?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const b={subjectSlug:fd.get('subjectSlug'),targetScore:+fd.get('targetScore'),examDate:fd.get('examDate'),daysPerWeek:+fd.get('daysPerWeek'),minutesPerDay:+fd.get('minutesPerDay')};const btn=e.currentTarget.querySelector('button');btn.disabled=true;btn.textContent='Строим…';try{await apiJson('/api/ai-pro/plan',{method:'POST',body:JSON.stringify(b)});notify('AI PRO обновлён');adminTools()}catch(err){notify(err.message);btn.disabled=false;btn.textContent='Построить план'}});$('[data-tool-diagnostic]')?.addEventListener('click',async()=>{try{await startSession('/api/ai-pro/diagnostic',{subjectSlug:d.subject,count:24},d.subject)}catch(e){notify(e.message)}});$('[data-tool-review]')?.addEventListener('click',async()=>{try{await startSession('/api/ai-pro/smart-review',{subjectSlug:d.subject,count:12},d.subject)}catch(e){notify(e.message)}});$('[data-tool-tutor]')?.addEventListener('click',async()=>{const s=d.tutor?.nextStep;if(!s)return;if(s.key==='theory'&&s.lessonId){location.hash=`lesson/${s.lessonId}`;return}if(s.key==='review'){try{return await startSession('/api/ai-pro/smart-review',{subjectSlug:d.subject,count:s.questions||12},d.subject)}catch(e){return notify(e.message)}}if(s.key==='practice'){const path=d.subject==='chemistry'?'/api/subjects/chemistry/training/sessions':'/api/training/sessions';const body=d.subject==='chemistry'?{examLine:d.tutor.line,mode:'adaptive',targetQuestions:s.questions||d.tutor.questions||8}:{subjectSlug:'biology',examLine:d.tutor.line,topicId:0,mode:'adaptive',targetQuestions:s.questions||d.tutor.questions||8};try{return await startSession(path,body,d.subject)}catch(e){return notify(e.message)}}});document.querySelectorAll('[data-map]').forEach(b=>b.onclick=()=>{location.hash=`progress-map/${b.dataset.map}`})}
})();
