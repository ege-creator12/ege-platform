const app=document.querySelector('#app'),toast=document.querySelector('#toast');let state={user:null,stats:null,topics:[],examDate:null,route:location.hash.slice(1)||'dashboard'};
const icons={dashboard:'⌂',biology:'⌘',chemistry:'⚗',practice:'✓',mocks:'◷',profile:'◎',admin:'◇'};
const api=async(path,opts={})=>{const r=await fetch('/api'+path,{headers:{'content-type':'application/json'},...opts});const data=await r.json();if(!r.ok)throw Error(data.error);return data};
const notify=m=>{toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2500)};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const navItems=()=>[['dashboard','Главная'],['biology','Биология'],['chemistry','Химия'],['mocks','Пробники'],['profile','Профиль'],...(state.user?.role==='admin'?[['admin','Управление']]:[])];
function shell(content){return `<div class="app"><aside class="sidebar"><div class="logo"><b>ОСНОВА</b> · ЕГЭ</div><nav>${navItems().map(([r,n])=>`<button data-nav="${r}" class="${state.route.startsWith(r)?'active':''}"><span class="nav-icon">${icons[r]}</span>${n}</button>`).join('')}</nav><div class="side-bottom"><div class="user-chip"><div class="avatar">${esc(state.user.name[0])}</div><div><b>${esc(state.user.name)}</b><small>${state.user.role==='admin'?'Администратор':state.user.level+' уровень'}</small></div></div></div></aside><main><div class="topbar"><button class="icon-btn" id="theme" title="Сменить тему">◐</button><button class="icon-btn" id="logout" title="Выйти">↪</button></div>${content}</main><div class="mobile-nav">${navItems().slice(0,4).map(([r,n])=>`<button data-nav="${r}"><span>${icons[r]}</span>${n}</button>`).join('')}</div></div>`}
function bindShell(){document.querySelectorAll('[data-nav]').forEach(x=>x.onclick=()=>go(x.dataset.nav));document.querySelector('#theme').onclick=()=>{document.documentElement.classList.toggle('dark');localStorage.theme=document.documentElement.classList.contains('dark')?'dark':'light'};document.querySelector('#logout').onclick=async()=>{await api('/logout',{method:'POST'});state.user=null;renderAuth()}}
function go(r){if(location.hash.slice(1)===r){state.route=r;return render()}location.hash=r}
function renderAuth(register=false){app.innerHTML=`<section class="auth"><div class="auth-art"><div class="brand">ОСНОВА · ЕГЭ</div><div><h1>Знания, которые остаются с тобой.</h1><p>Персональная подготовка к биологии и химии без хаоса: понятная теория, умные повторения и честная картина прогресса.</p></div><small>Биология и химия — единая система подготовки.</small></div><div class="auth-box"><form class="auth-form"><div class="brand">ОСНОВА</div><h2>${register?'Создать аккаунт':'С возвращением'}</h2><p>${register?'Начните системную подготовку уже сегодня.':'Продолжим подготовку с того места, где остановились.'}</p>${register?'<div class="field"><label>ИМЯ</label><input name="name" placeholder="Ваше имя" required></div>':''}<div class="field"><label>ЭЛЕКТРОННАЯ ПОЧТА</label><input name="email" type="email" placeholder="name@example.ru" required></div><div class="field"><label>ПАРОЛЬ</label><input name="password" type="password" minlength="8" placeholder="Не менее 8 символов" required></div><button class="btn block">${register?'Зарегистрироваться':'Войти'}</button><div class="switch">${register?'Уже есть аккаунт?':'Впервые здесь?'} <button class="link" type="button" id="switch">${register?'Войти':'Создать аккаунт'}</button></div></form></div></section>`;document.querySelector('#switch').onclick=()=>renderAuth(!register);document.querySelector('form').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target));try{await api(register?'/register':'/login',{method:'POST',body:JSON.stringify(b)});await boot()}catch(err){notify(err.message)}}}
const pct=n=>`<div class="progress"><i style="width:${n}%"></i></div>`;
function dashboard(){const s=state.stats,days=Math.max(0,Math.ceil((new Date(state.examDate)-new Date())/86400000)),weak=[...s.progress].sort((a,b)=>a.mastery-b.mastery).slice(0,3);return `<header><div class="eyebrow">Ваш учебный центр</div><h1>Добрый день, ${esc(state.user.name)}!</h1><div class="subtitle">Небольшой шаг сегодня — уверенный результат на экзамене.</div></header><div class="hero-row"><div class="card continue"><div class="eyebrow">Продолжить обучение</div><h2>${esc(weak[0]?.title||'Биология')}</h2><p>${weak[0]?.mastery||0}% освоено · задания подбираются по вашей истории</p>${pct(weak[0]?.mastery||0)}<button class="btn" data-train="${weak[0]?.id||0}">Начать занятие →</button></div><div class="card exam"><div class="eyebrow">До ЕГЭ</div><strong>${days}</strong><span>дней, чтобы подготовиться</span></div></div><div class="grid3"><div class="card metric"><div class="eyebrow">Общий прогресс</div><strong>${s.mastery}%</strong><span>по программе</span>${pct(s.mastery)}</div><div class="card metric"><div class="eyebrow">Серия занятий</div><strong>${s.streak} ${plural(s.streak,'день','дня','дней')}</strong><span>сохраняйте ритм</span></div><div class="card metric"><div class="eyebrow">Точность</div><strong>${s.accuracy}%</strong><span>${s.solved} заданий решено</span></div></div><div class="grid3"><section style="grid-column:span 2"><div class="section-head"><h2>План на сегодня</h2><span class="pill">15–25 минут</span></div><div class="card"><div class="list-row"><div><b>Повторить слабую тему</b><div class="subtitle">${esc(weak[0]?.title||'Начните первую тему')}</div></div><button class="btn ghost" data-train="${weak[0]?.id||0}">Перейти</button></div><div class="list-row"><div><b>Решить 5 новых заданий</b><div class="subtitle">Алгоритм не покажет уже изученное без причины</div></div><span class="pill">+100 XP</span></div></div></section><section><div class="section-head"><h2>Слабые темы</h2></div><div class="card weak-list">${weak.map(x=>`<div class="list-row"><span>${esc(x.title)}</span><b>${x.mastery}%</b></div>`).join('')}</div></section></div><div class="section-head"><h2>Последние результаты</h2></div><div class="card recent">${s.recent.length?s.recent.map(x=>`<div class="list-row"><div><b>${esc(x.topic)}</b><div class="subtitle">${esc(x.prompt.slice(0,70))}</div></div><span class="${x.correct?'good':'bad'}">${x.correct?'Верно':'Ошибка'}</span></div>`).join(''):'<div class="empty">Результаты появятся после первой тренировки</div>'}</div>`}
async function subject(slug){const d=await api('/subjects/'+slug);app.innerHTML=shell(`<header><div class="eyebrow">Предмет · ЕГЭ-2027</div><h1>${esc(d.subject.title)}</h1><div class="subtitle">${esc(d.subject.description)}</div></header><div class="section-head"><h2>Разделы курса</h2><span class="pill">${d.sections.length}</span></div><div class="topics">${d.sections.map((x,i)=>`<article class="card topic" data-section="${x.id}"><div class="topic-num">${String(i+1).padStart(2,'0')}</div><h3>${esc(x.title)}</h3><p>${esc(x.description)}</p><div class="topic-foot"><span>Открыть раздел</span><span>→</span></div></article>`).join('')}</div>`);bindShell();document.querySelectorAll('[data-section]').forEach(x=>x.onclick=()=>go('section/'+x.dataset.section))}
async function section(id){const d=await api('/sections/'+id);app.innerHTML=shell(`<button class="back" onclick="history.back()">← К предмету</button><header><div class="eyebrow">Раздел</div><h1>${esc(d.section.title)}</h1><p class="subtitle">${esc(d.section.description)}</p></header><div class="topics">${d.topics.map(x=>`<article class="card topic" data-topic="${x.id}"><h3>${esc(x.title)}</h3><p>${esc(x.description)}</p></article>`).join('')}</div>`);bindShell();document.querySelectorAll('[data-topic]').forEach(x=>x.onclick=()=>go('topic/'+x.dataset.topic))}
const blockView=b=>{const c=typeof b.content_json==='string'?JSON.parse(b.content_json):b.content_json;if(b.type==='heading')return `<h2>${esc(c.text)}</h2>`;if(b.type==='algorithm'||b.type==='list')return `<div class="lesson-block ${b.type}"><b>${esc(c.title||'Алгоритм')}</b><ol>${(c.items||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol></div>`;if(b.type==='image'||b.type==='diagram'){const src=c.path||(`/images/biology/${String(c.assetKey||'').replace(/^biology-/,'')}.svg`);return `<figure class="lesson-block visual"><img src="${esc(src)}" alt="${esc(c.alt||'Учебная схема')}" loading="lazy"><figcaption>${esc(c.alt||'')}</figcaption></figure>`}if(b.type==='table'||b.type==='comparison'){const rows=c.rows||c.items||[];return `<div class="lesson-table" role="region" aria-label="${esc(c.title||'Учебная таблица')}" tabindex="0"><table><thead><tr>${(c.columns||[]).map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${row.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}const label=b.type==='deep_dive'?'<span class="deep-label">Глубже ЕГЭ</span>':'';return `<div class="lesson-block ${b.type}">${label}${c.title?`<b>${esc(c.title)}</b>`:''}<p>${esc(c.text||c.question||'')}</p>${c.answer?`<small>Ответ: ${esc(c.answer)}</small>`:''}</div>`};
async function lesson(id){const d=await api('/lessons/'+id);app.innerHTML=shell(`<button class="back" onclick="history.back()">← К теме</button><article class="card lesson"><div class="eyebrow">Урок · ${d.lesson.estimated_minutes} минут</div><h1>${esc(d.lesson.title)}</h1><p class="subtitle">${esc(d.lesson.summary)}</p>${pct(d.progress.reading_progress||0)}<div class="lesson-content">${d.blocks.map(blockView).join('')}</div><div class="actions"><button class="btn" data-train="${d.lesson.topic_id}">Начать тренировку →</button><button class="btn ghost" id="complete-lesson">Отметить изученным</button></div></article>`);bindShell();bindTrain();document.querySelector('#complete-lesson').onclick=async e=>{e.target.disabled=true;await api('/lesson-progress/'+id,{method:'POST',body:JSON.stringify({readingProgress:100})});e.target.textContent='Урок изучен ✓'}}
async function topic(id){try{const d=await api('/topics/'+id);app.innerHTML=shell(`<button class="back" data-back>← Все темы</button><div class="card topic-hero"><div class="eyebrow">${esc(d.topic.subject)} · тема</div><h1>${esc(d.topic.title)}</h1><p class="subtitle">${esc(d.topic.description)}</p>${pct(d.topic.mastery)}</div><div class="grid3"><div class="card metric"><div class="eyebrow">Освоено</div><strong>${d.topic.mastery}%</strong></div><div class="card metric"><div class="eyebrow">Решено</div><strong>${d.stat.attempted}</strong><span>попыток</span></div><div class="card metric"><div class="eyebrow">Верно</div><strong>${d.stat.attempted?Math.round(d.stat.correct/d.stat.attempted*100):0}%</strong></div></div><div class="section-head"><h2>Краткая теория</h2></div><div class="card theory">${esc(d.topic.theory)}</div>${d.lessons?.length?`<div class="section-head"><h2>Уроки</h2></div><div class="topics">${d.lessons.map(x=>`<div class="card topic" data-lesson="${x.id}"><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p></div>`).join('')}</div>`:''}${d.children.length?`<div class="section-head"><h2>Подразделы</h2></div><div class="topics">${d.children.map(x=>`<div class="card topic" data-topic="${x.id}"><h3>${esc(x.title)}</h3><p>${esc(x.description)}</p></div>`).join('')}</div>`:''}<div class="section-head"><h2>Готовы проверить себя?</h2><button class="btn" data-train="${id}">Начать тренировку →</button></div>`);bindShell();document.querySelector('[data-back]').onclick=()=>d.topic.parent_id?go('topic/'+d.topic.parent_id):go('biology');document.querySelectorAll('[data-topic]').forEach(x=>x.onclick=()=>go('topic/'+x.dataset.topic));document.querySelectorAll('[data-lesson]').forEach(x=>x.onclick=()=>go('lesson/'+x.dataset.lesson));bindTrain()}catch(e){notify(e.message)}}
function profile(){const s=state.stats;return `<header><div class="eyebrow">Личный кабинет</div><h1>Ваш прогресс</h1></header><div class="profile-grid"><div class="card profile-card"><div class="avatar">${esc(state.user.name[0])}</div><h2>${esc(state.user.name)}</h2><div class="subtitle">${esc(state.user.email)}</div><p class="xp">Уровень ${state.user.level} · ${state.user.xp} XP</p>${pct(state.user.xp%500/5)}</div><div class="grid3"><div class="card metric"><span>Решено</span><strong>${s.solved}</strong></div><div class="card metric"><span>Точность</span><strong>${s.accuracy}%</strong></div><div class="card metric"><span>Серия</span><strong>${s.streak}</strong></div></div></div><div class="section-head"><h2>Прогресс по темам</h2></div><div class="card bars">${s.progress.map(x=>`<div><div class="bar-label"><b>${esc(x.title)}</b><span>${x.mastery}%</span></div>${pct(x.mastery)}</div>`).join('')}</div><div class="section-head"><h2>История занятий</h2></div><div class="card">${s.activity.length?s.activity.map(x=>`<div class="list-row"><span>${new Date(x.day).toLocaleDateString('ru-RU',{day:'numeric',month:'long'})}</span><b>${x.solved} ${plural(x.solved,'задание','задания','заданий')}</b></div>`).join(''):'<div class="empty">Начните тренировку, чтобы сформировать историю</div>'}</div>`}
function mocks(){return `<header><div class="eyebrow">Экзаменационный режим</div><h1>Пробные варианты</h1><div class="subtitle">Тренируйте распределение времени и знакомьтесь со структурой экзамена.</div></header><div class="section-head"><h2>Доступные варианты</h2></div><div class="card mock"><div><strong>Диагностический вариант №1</strong><p class="subtitle">28 заданий · 3 часа 30 минут · автоматический разбор ошибок</p></div><span class="tag-soon">СКОРО</span></div><div class="section-head"><h2>Как проходит пробник</h2></div><div class="grid3"><div class="card metric"><div class="eyebrow">01</div><strong>Таймер</strong><span>реальные условия экзамена</span></div><div class="card metric"><div class="eyebrow">02</div><strong>Навигация</strong><span>возвращайтесь к заданиям</span></div><div class="card metric"><div class="eyebrow">03</div><strong>Разбор</strong><span>результат и работа над ошибками</span></div></div>`}
function practice(){const modes=[['adaptive','Умная тренировка','Слабые места, новые задания и повторы в оптимальном порядке.'],['new','Только новое','Задания, которые вы ещё не решали.'],['review','Повторение','Задания, срок повторения которых уже наступил.'],['mistakes','Работа над ошибками','Вернитесь к последним неверным ответам.']];return `<header><div class="eyebrow">Персональная практика</div><h1>Как потренируемся?</h1><div class="subtitle">Выберите режим — состав сессии зафиксируется, а прогресс сохранится после каждого ответа.</div></header><div class="practice-modes">${modes.map(([mode,title,text],i)=>`<article class="card practice-mode ${i===0?'recommended':''}"><div class="eyebrow">${i===0?'Рекомендуем':'Режим'}</div><h2>${title}</h2><p class="subtitle">${text}</p><button class="btn ${i?'ghost':''}" data-mode="${mode}">Начать · 10 заданий</button></article>`).join('')}</div>`}
async function admin(){const d=await api('/admin/stats'),q=await api('/admin/questions');app.innerHTML=shell(`<header><div class="eyebrow">Администрирование</div><h1>Управление платформой</h1></header><div class="grid3"><div class="card metric"><span>Ученики</span><strong>${d.counts.students}</strong></div><div class="card metric"><span>Задания</span><strong>${d.counts.questions}</strong></div><div class="card metric"><span>Попытки</span><strong>${d.counts.attempts}</strong></div></div><div class="section-head"><h2>База заданий</h2><button class="btn" id="addq">+ Добавить</button></div><div class="card" style="overflow:auto"><table class="admin-table"><thead><tr><th>Задание</th><th>Тема</th><th>Тип</th><th>Сложность</th><th></th></tr></thead><tbody>${q.questions.map(x=>`<tr><td>${esc(x.prompt.slice(0,65))}</td><td>${esc(x.topic)}</td><td>${x.type}</td><td>${'●'.repeat(x.difficulty)}</td><td><button class="link del" data-id="${x.id}">Удалить</button></td></tr>`).join('')}</tbody></table></div><div class="section-head"><h2>Ученики</h2></div><div class="card" style="overflow:auto"><table class="admin-table"><thead><tr><th>Имя</th><th>Почта</th><th>XP</th><th>Решено</th><th>Точность</th></tr></thead><tbody>${d.users.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.email)}</td><td>${x.xp}</td><td>${x.solved}</td><td>${x.accuracy}%</td></tr>`).join('')}</tbody></table></div>`);bindShell();document.querySelectorAll('.del').forEach(x=>x.onclick=async()=>{if(confirm('Удалить задание?')){await api('/admin/questions/'+x.dataset.id,{method:'DELETE'});admin()}});document.querySelector('#addq').onclick=()=>showQuestionForm()}
function showQuestionForm(){app.innerHTML=shell(`<button class="back" onclick="location.hash='admin'">← Назад</button><div class="card" style="max-width:720px;margin:25px auto"><div class="eyebrow">Новое задание</div><h2>Добавить в базу</h2><form id="qform"><div class="field"><label>ТЕМА</label><select name="topicId">${state.topics.map(x=>`<option value="${x.id}">${esc(x.title)}</option>`).join('')}</select></div><div class="field"><label>ТИП</label><select name="type"><option value="single">Один ответ</option><option value="multiple">Несколько ответов</option><option value="text">Краткий ответ</option><option value="sequence">Последовательность</option><option value="matching">Соответствие</option></select></div><div class="field"><label>УСЛОВИЕ</label><textarea name="prompt" rows="4" required></textarea></div><div class="field"><label>ВАРИАНТЫ (каждый с новой строки)</label><textarea name="options" rows="5"></textarea></div><div class="field"><label>ОТВЕТ (индексы 0,1... через запятую или текст)</label><input name="answer" required></div><div class="field"><label>ОБЪЯСНЕНИЕ</label><textarea name="explanation" rows="4" required></textarea></div><div class="field"><label>СЛОЖНОСТЬ</label><select name="difficulty"><option>1</option><option>2</option><option>3</option></select></div><button class="btn">Сохранить задание</button></form></div>`);bindShell();document.querySelector('#qform').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));x.options=x.options.split('\n').map(v=>v.trim()).filter(Boolean);x.answer=x.answer.split(',').map(v=>v.trim());try{await api('/admin/questions',{method:'POST',body:JSON.stringify(x)});notify('Задание добавлено');go('admin')}catch(err){notify(err.message)}}}
function bindTrain(){document.querySelectorAll('[data-train]').forEach(x=>x.onclick=()=>startTraining(Number(x.dataset.train),'topic'));document.querySelectorAll('[data-mode]').forEach(x=>x.onclick=()=>startTraining(0,x.dataset.mode))}
async function startTraining(topicId=0,mode='adaptive'){try{const created=await api('/training/sessions',{method:'POST',body:JSON.stringify({topicId,mode,targetQuestions:10})});sessionStorage.trainingSession=created.session.id;state.route='training';history.replaceState(null,'','#training');return trainingQuestion(created.session.id)}catch(e){notify(e.message)}}
async function trainingQuestion(sessionId){let started=Date.now();try{const d=await api(`/training/sessions/${sessionId}/next`);if(d.done)return trainingSummary(d.session);const q=d.question,progress=Math.round(d.session.answeredCount/d.session.targetQuestions*100),extended=q.questionType==='extended_answer';let controls=q.type==='text'?`<textarea class="answer-input" id="textanswer" placeholder="${extended?'Сформулируйте развёрнутый ответ':'Введите краткий ответ'}" autocomplete="off"></textarea>`:`<div class="options">${d.options.map((o,i)=>`<label class="option"><input type="${q.type==='multiple'||q.type==='matching'?'checkbox':'radio'}" name="answer" value="${esc(o.value)}"><b>${i+1}</b><span>${esc(o.label)}</span></label>`).join('')}</div>`;app.innerHTML=shell(`<div class="training"><div class="train-top"><button class="back" data-exit>← Завершить</button><span class="pill">${d.session.answeredCount+1} из ${d.session.targetQuestions}</span></div>${pct(progress)}<div class="card question-card"><div class="q-meta"><span class="pill">${extended?'Развёрнутый ответ':({single:'Один ответ',multiple:'Несколько ответов',text:'Краткий ответ',sequence:'Последовательность',matching:'Соответствие'})[q.type]}</span><span class="pill">${esc(q.topic||'Биология')}</span><span class="pill">+20 XP</span></div>${q.instruction?`<p class="question-instruction">${esc(q.instruction)}</p>`:''}<h2>${esc(q.prompt)}</h2>${controls}<div class="actions training-actions"><button class="btn ghost reveal-btn" id="reveal">${extended?'Показать эталон ответа и разбор':'Не знаю — показать разбор'}</button><button class="btn" id="submit">Проверить ответ</button></div></div></div>`);bindShell();document.querySelector('[data-exit]').onclick=()=>finishTraining(sessionId);document.querySelectorAll('.option').forEach(x=>x.onclick=()=>setTimeout(()=>x.classList.toggle('selected',x.querySelector('input').checked),0));const lock=()=>document.querySelectorAll('#submit,#reveal').forEach(x=>x.disabled=true);document.querySelector('#reveal').onclick=async()=>{lock();try{const r=await api(`/training/sessions/${sessionId}/reveal`,{method:'POST',body:JSON.stringify({questionId:q.id,duration:Math.round((Date.now()-started)/1000)})});state.stats=r.stats;trainingFeedback(sessionId,r)}catch(e){notify(e.message)}};document.querySelector('#submit').onclick=async()=>{const answer=q.type==='text'?[document.querySelector('#textanswer').value]:[...document.querySelectorAll('input[name=answer]:checked')].map(x=>x.value);if(!answer.filter(Boolean).length)return notify('Выберите или введите ответ');lock();try{const r=await api(`/training/sessions/${sessionId}/answer`,{method:'POST',body:JSON.stringify({questionId:q.id,answer,duration:Math.round((Date.now()-started)/1000)})});state.stats=r.stats;state.user.xp+=r.xp;trainingFeedback(sessionId,r)}catch(e){notify(e.message)}}}catch(e){notify(e.message);go('practice')}}
function reviewAnswerHtml(result){const review=result.reviewAnswer||{label:'Правильный ответ',examAnswer:(result.expected||[]).join(', '),items:[]},items=Array.isArray(review.items)?review.items:[];return `<section><h3>${esc(review.label||'Правильный ответ')}</h3><p class="exam-answer">${esc(review.examAnswer||'—')}</p></section>${items.length?`<section><h3>Расшифровка</h3><div class="answer-decoding">${items.map(item=>`<div>${esc(item)}</div>`).join('')}</div></section>`:''}`}
function trainingFeedback(sessionId,result){const revealed=result.resolutionType==='revealed',steps=Array.isArray(result.solutionSteps)?result.solutionSteps.filter(step=>typeof step==='string'?step.trim():step?.text||step?.description):[];app.innerHTML=shell(`<div class="training"><div class="card question-card"><div class="eyebrow">${revealed?'Ответ открыт — задание добавлено в повторение.':'Ответ сохранён'}</div><h1>${result.correct?'Верно!':revealed?'Разбор задания':'Пока неверно'}</h1><div class="result ${result.correct?'':'wrong'}">${result.correct?`<b>+${result.xp} XP</b>`:''}${reviewAnswerHtml(result)}${result.explanation?`<section><h3>Почему так</h3><p>${esc(result.explanation)}</p></section>`:''}${steps.length?`<section><h3>Решение по шагам</h3><ol>${steps.map(step=>`<li>${esc(typeof step==='string'?step:step.text||step.description)}</li>`).join('')}</ol></section>`:''}${result.maxScore>1?`<p class="score-note">Максимальный балл: ${result.maxScore}</p>`:''}</div><p class="subtitle">Повторение запланировано через ${result.nextReviewInDays} ${plural(result.nextReviewInDays,'день','дня','дней')}.</p><div class="actions"><button class="btn" id="next-question">${result.done?'Итоги сессии':'Следующее задание →'}</button><button class="btn ghost" id="finish-training">Завершить</button></div></div></div>`);bindShell();document.querySelector('#next-question').onclick=()=>result.done?trainingSummary(result.session):trainingQuestion(sessionId);document.querySelector('#finish-training').onclick=()=>finishTraining(sessionId)}
async function finishTraining(sessionId){try{const d=await api(`/training/sessions/${sessionId}/finish`,{method:'POST'});trainingSummary(d.session)}catch{go('dashboard')}}
function trainingSummary(session){sessionStorage.removeItem('trainingSession');const accuracy=session.answered_count?Math.round(session.correct_count/session.answered_count*100):0;app.innerHTML=shell(`<div class="training"><div class="card question-card summary-card"><div class="eyebrow">Тренировка завершена</div><h1>${session.answered_count?'Отличная работа!':'Вернёмся, когда будете готовы'}</h1><div class="grid3"><div class="metric"><strong>${session.answered_count}</strong><span>ответов</span></div><div class="metric"><strong>${session.correct_count}</strong><span>верно</span></div><div class="metric"><strong>${accuracy}%</strong><span>точность</span></div></div><div class="actions"><button class="btn" id="to-dashboard">На главную</button><button class="btn ghost" id="repeat">Ещё тренировка</button></div></div></div>`);bindShell();document.querySelector('#to-dashboard').onclick=()=>go('dashboard');document.querySelector('#repeat').onclick=()=>go('practice')}
function plural(n,a,b,c){n=Math.abs(n)%100;const n1=n%10;return n>10&&n<20?c:n1>1&&n1<5?b:n1===1?a:c}
async function render(){if(!state.user)return renderAuth();if(state.route==='biology'||state.route==='chemistry')return subject(state.route);if(state.route.startsWith('section/'))return section(state.route.split('/')[1]);if(state.route.startsWith('lesson/'))return lesson(state.route.split('/')[1]);if(state.route.startsWith('topic/'))return topic(state.route.split('/')[1]);if(state.route==='training'){const id=Number(sessionStorage.trainingSession);return id?trainingQuestion(id):go('practice')}if(state.route==='admin')return admin();if(!['dashboard','practice','profile','mocks'].includes(state.route))state.route='dashboard';let content=state.route==='practice'?practice():state.route==='profile'?profile():state.route==='mocks'?mocks():dashboard();app.innerHTML=shell(content);bindShell();document.querySelectorAll('[data-topic]').forEach(x=>x.onclick=()=>go('topic/'+x.dataset.topic));bindTrain()}
async function boot(){if(localStorage.theme==='dark')document.documentElement.classList.add('dark');try{const [m,t]=await Promise.all([api('/me'),api('/topics')]);Object.assign(state,{user:m.user,stats:m.stats,examDate:m.examDate,topics:t.topics});render()}catch{renderAuth()}}
addEventListener('hashchange',()=>{state.route=location.hash.slice(1)||'dashboard';render()});boot();
